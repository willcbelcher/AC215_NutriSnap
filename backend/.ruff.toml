# Ruff configuration for backend
# Relaxed linting - errors only (E, F)

[lint]
select = ["E", "F"]  # Only errors and fatal issues
ignore = ["E501", "E402"]  # Ignore line length and import order (style, not errors)
