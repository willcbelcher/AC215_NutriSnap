# Ruff configuration for ML pipeline
# Relaxed linting - errors only (E, F)

[lint]
select = ["E", "F"]  # Only errors and fatal issues
ignore = ["E501", "E402", "F401", "F841"]  # Ignore line length, import order, unused imports/vars
